import { Invitation } from "@/types/invitation";
import React from "react";

export const Destinations = ({ dev, invitation }: { dev: boolean; invitation: Invitation | null }) => {
  return <span>Hello destinations</span>;
};
