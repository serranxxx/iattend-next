import { Invitation } from "@/types/invitation";
import React from "react";

export const Gifts = ({ dev, invitation }: { dev: boolean; invitation: Invitation | null }) => {
  return <span>Hello gifts</span>;
};
