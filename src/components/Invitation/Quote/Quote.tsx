import { Invitation } from "@/types/invitation";
import React from "react";

export const Quote = ({ dev, invitation }: { dev: boolean; invitation: Invitation | null }) => {
  return <span>Hello quote</span>;
};
