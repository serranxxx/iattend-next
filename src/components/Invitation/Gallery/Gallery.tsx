import { Invitation } from "@/types/invitation";
import React from "react";

export const Gallery = ({ dev, invitation }: { dev: boolean; invitation: Invitation | null }) => {
  return <span>Hello gallery</span>;
};
