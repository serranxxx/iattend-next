import { Invitation } from "@/types/invitation";
import React from "react";

export const Notices = ({ dev, invitation }: { dev: boolean; invitation: Invitation | null }) => {
  return <span>Hello notices</span>;
};
