import { Invitation } from "@/types/invitation";
import React from "react";

export const DressCode = ({ dev, invitation }: { dev: boolean; invitation: Invitation | null }) => {
  return <span>Hello dresscode</span>;
};
