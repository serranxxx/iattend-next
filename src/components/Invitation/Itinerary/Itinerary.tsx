import { Invitation } from "@/types/invitation";
import React from "react";

export const Itinerary = ({ dev, invitation }: { dev: boolean; invitation: Invitation | null }) => {
  return <span>Hello itinerary</span>;
};
